<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Load Pusher properties file -->
    <context:property-placeholder location="classpath:pusher.properties" ignore-unresolvable="true" />

    <!-- Pusher Real-time Notification Client Config -->
    <bean id="pusher" class="com.pusher.rest.Pusher">
        <constructor-arg index="0" value="${pusher.appId}" />
        <constructor-arg index="1" value="${pusher.key}" />
        <constructor-arg index="2" value="${pusher.secret}" />
        <property name="cluster" value="${pusher.cluster}" />
    </bean>

</beans>
